export const siteConfig = {
  name: "PaceUp",
  url: "https://paceup.app",
  description: "Track and analyze your Strava running activities.",
  baseLinks: {
    dashboard: "/dashboard",
    activities: "/activities",
    sync: "/sync",
  },
}

export type siteConfig = typeof siteConfig
